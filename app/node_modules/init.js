/**
 * 初始化模块，程序开始运行时调用
 */
require('./config').init();
const db = require("./db");
const cache = require('./cache');
const logger = require('./logger');

module.exports=function(call){
    db.init();
    cache.clear();
    logger.init();
    call();
};