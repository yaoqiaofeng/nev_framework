/**
 * 配置模块，全局使用到的变量都要在此处配置
 */
const conf = require("../../config");

var config = {
    init: function (){
        for (let item in conf){
            this[item] = conf[item];
        }
        console.log(this);
    }
};

global.$config = config;

module.exports=config;
